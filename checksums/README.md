Before moving forward, we will run checksums to ensure that the sequencing data transferred from McGill with Globus was not 
corrupted.

The .md5 files from McGill contained path names that are no longer valid. We want to remove these so that the .md5 files 
contain only the checksums string and the file name.
